import React from "react"


export default function Buttons() {
    return (
        <section id="buttons-section">
            <button>Start</button>
            <button>Set</button>
            <button>Stop</button>
        </section>
    )
}